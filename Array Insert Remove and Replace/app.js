let arr = [1, 2, 3, 5, 6, 8, 9, 10, 13, 14, 15, 19, 20];

//arr.push(50);// insert last data
//arr.unshift(50);// insert first data
//arr.splice(3, 0, 50, 30);// insert spesific data
//arr[3] = undefined;// remove data
//arr.pop(); //remove last data
//arr.shift();// rimove first data

//arr.splice(3, 1); remove spesific data
console.log(arr);
